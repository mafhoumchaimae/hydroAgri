% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_weather_data.R
\name{import_weather_data}
\alias{import_weather_data}
\title{Importer les donnees meteorologiques}
\usage{
import_weather_data(
  file = NULL,
  format = "csv",
  fill_missing = "interpolate",
  data = NULL
)
}
\arguments{
\item{file}{Chemin vers le fichier CSV ou Excel}

\item{format}{Format du fichier : "csv" ou "excel". Par defaut "csv"}

\item{fill_missing}{Methode pour les valeurs manquantes :
"interpolate" ou "mean". Par defaut "interpolate"}
}
\value{
Un dataframe avec les colonnes :
date, Tmax, Tmin, Tmean, RH, Wind, Rs, Rain
}
\description{
Importe et prepare les donnees meteo depuis un fichier CSV ou Excel.
Gere les dates, detecte les valeurs manquantes et convertit les unites.
}
\examples{
meteo <- data.frame(
  date = seq(as.Date("2023-01-01"), as.Date("2023-12-31"), by = "day"),
  Tmax = runif(365, 20, 38),
  Tmin = runif(365, 10, 22),
  RH   = runif(365, 40, 80),
  Wind = runif(365, 1, 4),
  Rs   = runif(365, 15, 25),
  Rain = ifelse(runif(365) > 0.85, runif(365, 0, 20), 0)
)
weather <- import_weather_data(data = meteo, format = "dataframe")
head(weather)

}
